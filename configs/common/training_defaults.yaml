seeds: [17, 42, 3407]
epochs: 100
batch_size: 2
gradient_accumulation_steps: 8
use_amp: true
num_workers: 2
learning_rate: 0.0001
weight_decay: 0.05
warmup_epochs: 3
max_grad_norm: 1.0
save_every_epochs: 1
validate_every_epochs: 1
tracker: tensorboard
architecture_recipe: shared_controlled
