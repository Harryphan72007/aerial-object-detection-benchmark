# Published VisDrone results

The CSV intentionally starts empty except for its schema. Add only values transcribed from primary papers or official repositories. Use `not reported` or an empty field rather than estimating. Record class count, split, resolution, external data, tiling, TTA, ensemble status, hardware, and source URL so incomparable settings are not merged.
