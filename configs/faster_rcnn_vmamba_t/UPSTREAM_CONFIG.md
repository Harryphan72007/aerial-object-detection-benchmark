The runtime loads `${VMAMBA_ROOT}/detection/configs/vssm/mask_rcnn_vssm_fpn_coco_tiny.py`, imports `${VMAMBA_ROOT}/detection/model.py` to register `MM_VSSM`, switches the detector to Faster R-CNN, and removes the inherited mask branch. No undocumented VMamba module names are assumed for hooks; visualization discovers modules from `named_modules()` and requires the user to select actual names printed by the notebook.
